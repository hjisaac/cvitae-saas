[project]
name = "cvitae-saas-backend"
version = "0.1.0"
description = "FastAPI wrapper for CVitae engine"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
    "cvitae",
    "pydantic-i18n",
]

[tool.uv.sources]
cvitae = { path = "../core-engine", editable = true }
